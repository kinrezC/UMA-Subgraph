{
  "name": "UMA-Tracker",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ UMA-Tracker",
    "create-local": "graph create --node http://localhost:8020/ UMA-Tracker",
    "remove-local": "graph remove --node http://localhost:8020/ UMA-Tracker",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 UMA-Tracker"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.16.2",
    "@graphprotocol/graph-ts": "0.16.0"
  }
}
